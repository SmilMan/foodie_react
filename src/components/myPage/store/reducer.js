let defaultData = {
    
}

export default (state, action) => {
    // let newData = JSON.parse(JSON.stringify(defaultData));
    // if (action.type === "init_username") {
    //     newData.userName = action.name;
    //     return newData;
    // }
    return defaultData;
}